# 校准链路文档

## 一、总体架构

校准流程涉及 PC 端与 Device 端双向通信，通过三路 TCP 完成：
- **Control 9000**：命令/响应（PC → Device 发起校准、查询校准时间）
- **Status 9001**：状态推送（Device → PC 心跳、扫描状态）
- **Stream 9002**：校准进度/结果推送（Device → PC 进度和完成摘要）

---

## 二、数据流总览

```
┌─────────────────────────────────────────────────────────────────┐
│ PC 端 QML (InstrumentCalibrationPop.qml)                        │
│   用户选择通道 + 校准类型（透射光 / 背射光）                      │
│   → experiment_ctrl.startCalibration(channel, "transmission")    │
└────────────────────────────┬────────────────────────────────────┘
                             │ Control 9000
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│ Device DataTransmitController (datatransmitcontroller.cpp)       │
│   解析 "start_calibration" 命令                                  │
│   → emit calibrationRequested(channel, calibrationType, reqId)   │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│ Device ExperimentCtrl (experiment_ctrl.cpp)                      │
│   startCalibration(channel, calibrationType)                     │
│   → 初始化 CalibrationScanState (3轮，0~55mm，20μm)             │
│   → triggerNextCalibrationScan() × 3                             │
└────────────────────────────┬────────────────────────────────────┘
                             │
              ┌──────────────┴──────────────┐
              ▼                             ▼
┌─────────────────────────┐    ┌──────────────────────────┐
│ pollChannelStatus (轮询) │    │ triggerNextCalibrationScan│
│ → 检测 A/B 存储区可读数  │    │ → set_scan_range(0,55)   │
│ → tryFetchCalibrationData│    │ → set_step(20)           │
│ → 累积到 scanRoundRows   │    │ → start_scan(1)          │
│ → 判断本轮完成           │    │ → emit calibrationProgress│
└─────────────────────────┘    └──────────────────────────┘
              │                       │            Stream 9002
              ▼                       ▼
┌─────────────────────────────────────────────────────────────────┐
│ computeCalibrationAverage()                                      │
│   → 按索引对齐 3 轮数据求平均                                     │
│   → 合并已有校准数据（另一光类型）                                │
│   → clearCalibrationAvgDataByChannel + batchAddCalibrationAvgData │
│   → insertCalibrationMeta (写入校准时间到 calibration_meta 表)   │
│   → emit calibrationCompleted                                    │
│     ──── Stream 9002 ────▶ PC 端 InstrumentCalibrationPop 显示结果│
└─────────────────────────────────────────────────────────────────┘

═════════════════════════════════════════════════════════════════════

实验启动时：
  startExperiment()
    → loadCalibrationAvgTable(channel, dbManager)
      → 从 calibration_avg_data 表加载到内存
    → 实验中按高度 O(1) 查询校准参考值

═════════════════════════════════════════════════════════════════════

校准时间查询（PC 端 & Device 端）：
  QML refreshCalibrationTime()
    → experiment_ctrl.getLastCalibrationTime(channel, "transmission")
    → experiment_ctrl.getLastCalibrationTime(channel, "backscatter")
      ┌─ Device 端：直接查 SqlOrmManager → SELECT MAX(calibrated_at)
      │            FROM calibration_meta WHERE channel=? AND calibration_type=?
      └─ PC 端：  sendRequestAndWait("get_calibration_time")
                    → Control 9000 → Device calibrationTimeRequested
                    → ControllerManager → getLastCalibrationTime → sendCommandResult
```

---

## 三、阶段 1：原始数据读取

### 3.1 入口 — 状态轮询触发

**文件**：[experiment_ctrl.cpp:L1231-L1257](StabilityAnalyzer_Device/SubApplication/ANALYZER/MainWindow/src/Controller/experiment_ctrl.cpp)

校准模式下，轮询下位机状态寄存器，检测到 `storageAReadableCount > 0 || storageBReadableCount > 0` 时触发数据读取。

```cpp
if (m_calibrationModes.value(ch, false)) {
    // 读取 A/B 存储区可读数量和状态
    // 有数据 → tryFetchCalibrationData()
    // 无数据 + 设备空闲(runStatus==0) + 有当轮数据
    //   → 本轮完成：round<3 触发下一轮 / round==3 计算平均值
}
```

### 3.2 底层 Modbus 读取

**文件**：[experiment_data_service.cpp:L202-L310](StabilityAnalyzer_Device/SubApplication/ANALYZER/MainWindow/src/Common/experiment_data_service.cpp)

- A 区（索引 0~249）：分 5 批读取，每批 100 word（50 对）
- B 区（索引 250~499）：分 5 批读取，每批 100 word（50 对）
- 哪个区可读就取哪个，**取完回写状态值 3**
- 数据通过回调返回，**不入库、不写内存缓存**

### 3.3 原始数据解析

**文件**：[experiment_session_service.cpp:L245-L265](StabilityAnalyzer_Device/SubApplication/ANALYZER/MainWindow/src/Common/experiment_session_service.cpp)

每个 Modbus word 对 `(raw[2i], raw[2i+1])` 对应一个采样点：

| 寄存器位 | 字段 | 含义 |
|----------|------|------|
| raw[2i] | transmission_intensity | 透射光强（原始值） |
| raw[2i+1] | backscatter_intensity | 背射光强（原始值） |

高度计算：`height = (scanRangeStartMm * 1000 + (区域起始索引 + i) * 20.0) / 1000`

- A 区起始索引 = 0，B 区起始索引 = 250
- 固定范围 0~55mm，步长 20μm → 共 2751 个采样点

---

## 四、阶段 2：三次扫描数据累积

### 4.1 数据结构

**文件**：[experiment_types.h:L91-L100](StabilityAnalyzer_Device/SubApplication/ANALYZER/MainWindow/inc/Common/experiment_types.h)

```cpp
struct CalibrationScanState {
    int scanRound = 0;                              // 当前轮次（1-based）
    int totalRounds = 3;                           // 总轮数固定 3
    double scanRangeStartMm = 0.0;                 // 固定 0mm
    double scanEndMm = 55.0;                       // 固定 55mm
    double scanStepUm = 20.0;                      // 固定 20μm
    QString calibrationType;                       // "transmission" / "backscatter"
    QVector<QVector<QVariantMap>> scanRoundRows;   // [轮次][行数据]
};
```

### 4.2 累积逻辑

**文件**：[experiment_ctrl.cpp:L858-L867](StabilityAnalyzer_Device/SubApplication/ANALYZER/MainWindow/src/Controller/experiment_ctrl.cpp)

```cpp
// 每批数据追加到当前轮次
mutableState.scanRoundRows[roundIndex] += batchRows;
```

结构示意：
```
scanRoundRows = [
    [row0, row1, ... row2750],   // 第1轮
    [row0, row1, ... row2750],   // 第2轮
    [row0, row1, ... row2750],   // 第3轮
]
```

每轮判断完成条件：`hasCurrentRoundData && runStatus==0 && noMoreData`

---

## 五、阶段 3：按高度对齐求平均

**文件**：[experiment_ctrl.cpp:L874-L993](StabilityAnalyzer_Device/SubApplication/ANALYZER/MainWindow/src/Controller/experiment_ctrl.cpp)

### 5.1 对齐方式

按**数组索引对齐**（3 次扫描范围完全相同，同索引 = 同一高度）：

```cpp
for (int i = 0; i < pointCount; ++i) {
    // 同一索引位置，跨轮次求和
    sumTrans = round0[i].trans + round1[i].trans + round2[i].trans
    sumBack  = round0[i].back + round1[i].back + round2[i].back
    avgTrans = sumTrans / validCount
    avgBack  = sumBack  / validCount
}
```

### 5.2 合并另一光类型

先加载数据库中已有的校准数据，用微米级整数（`qRound64(height * 1000)`）做 key 避免浮点精度问题：

```cpp
const QVector<QVariantMap> existingRows = m_dbManager->getCalibrationAvgDataByChannel(channel);
QMap<qint64, QVariantMap> existingByHeightUm;

// 透射校准：透射存新值，背射保留已有值
if (isTransmission) {
    entry["avg_transmission_intensity"] = avgTrans;            // ← 新平均值
    entry["avg_backscatter_intensity"] = existingOldBackscatter; // ← 保留旧值
}
// 背射校准：背射存新值，透射保留已有值
else {
    entry["avg_backscatter_intensity"] = avgBack;              // ← 新平均值
    entry["avg_transmission_intensity"] = existingOldTransmission; // ← 保留旧值
}
```

### 5.3 补齐逻辑

本次扫描高度范围外的已有数据原样补回，防止范围不一致时数据丢失：

```cpp
for (auto it = existingByHeightUm.constBegin(); ...) {
    if (!coveredHeightUm.contains(it.key())) {
        avgEntries.append(it.value());  // 原样补回
    }
}
```

---

## 六、阶段 4：写入数据库

### 6.1 calibration_avg_data 表结构

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 自增主键 |
| channel | INTEGER | 通道号 0~3 |
| height | REAL | 高度（mm） |
| avg_transmission_intensity | REAL | 透射光强平均值 |
| avg_backscatter_intensity | REAL | 背射光强平均值 |
| scan_count | INTEGER | 有效扫描轮数 |
| created_at | TEXT | 写入时间 |

**关键设计**：透射光和背射光校准值在同一行里，不在分开两行。分开校准时通过"合并已有数据"机制保证另一种光的值不丢失。

### 6.2 calibration_meta 表结构（校准时间元数据）

**文件**：[SqlOrmManager.cpp:L211-L216](StabilityAnalyzer_Device/SqlOrm/src/SqlOrmManager.cpp)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 自增主键 |
| channel | INTEGER | 通道号 0~3 |
| calibration_type | TEXT | `"transmission"` 或 `"backscatter"` |
| calibrated_at | TEXT | 校准完成时间 `"yyyy-MM-dd hh:mm:ss"` |

**设计目的**：透射光和背射光分开校准，各自最后一次的时间独立持久化。每次校准完成时 INSERT 一条新记录（不覆盖），查询时取 `MAX(calibrated_at)` 按类型筛选。

### 6.3 写入流程

**文件**：[SqlOrmManager.cpp:L2229-L2260](StabilityAnalyzer_Device/SqlOrm/src/SqlOrmManager.cpp) 及 L2344-L2375（校准元数据）

```
1. 加载已有数据到内存（existingByHeightUm）
2. 构造新条目（当前类型新值 + 另一类型已有值）
3. 补齐高度范围外的已有数据
4. 按高度排序
5. clearCalibrationAvgDataByChannel(channel)  → DELETE 旧校准平均值
6. batchAddCalibrationAvgData(avgEntries)      → INSERT 新校准平均值（事务内）
7. insertCalibrationMeta(channel, type, time)  → INSERT 校准时间元数据
```

### 6.4 校准完成信号

**文件**：[experiment_ctrl.cpp:L995-L1010](StabilityAnalyzer_Device/SubApplication/ANALYZER/MainWindow/src/Controller/experiment_ctrl.cpp)

通过 Stream 9002 推送 `calibration_completed` 消息，携带 summary：

```json
{
  "type": "calibration_completed",
  "channel": 0,
  "summary": {
    "channel": 0,
    "calibration_type": "transmission",
    "total_points": 2751,
    "scan_rounds": 3,
    "overall_avg_transmission": 1234.56,
    "overall_avg_backscatter": 0.0,
    "max_transmission": 1500.0,
    "min_transmission": 800.0,
    "max_backscatter": 0.0,
    "min_backscatter": 0.0,
    "calibrated_at": "2026-05-28 14:30:00"
  }
}
```

PC 端 [experiment_ctrl.cpp:L98-L115](StabilityAnalyzer_PC/SubApplication/ANALYZER/MainWindow/src/Controller/experiment_ctrl.cpp) 接收后 emit 对应信号给 QML。

---

## 七、阶段 5：实验时查询校准参考值

### 7.1 实验启动时加载

**文件**：[experiment_session_service.cpp:L106-L137](StabilityAnalyzer_Device/SubApplication/ANALYZER/MainWindow/src/Common/experiment_session_service.cpp)

`loadCalibrationAvgTable(channel, dbManager)` 从 `calibration_avg_data` 表全量加载到内存：

```cpp
m_calibrationAvgTables[channel] = [
    { height: 0.00,   avgTrans: xxx, avgBack: xxx },
    { height: 0.02,   avgTrans: xxx, avgBack: xxx },
    ...
    { height: 55.00,  avgTrans: xxx, avgBack: xxx },
]
```

### 7.2 按高度 O(1) 查询

**文件**：[experiment_session_service.cpp:L141-L170](StabilityAnalyzer_Device/SubApplication/ANALYZER/MainWindow/src/Common/experiment_session_service.cpp)

等间距直接索引计算（步长 20μm），无需遍历或二分查找：

```cpp
idx = qRound((heightUm - firstHeightUm) / 20.0)
return table[idx].avgTransmission   // 或 avgBackscatter
```

校准表为空时返回 `0.0`。

---

## 八、阶段 6：校准时间查询与显示

### 8.1 数据库查询

**文件**：[SqlOrmManager.cpp:L2377-L2397](StabilityAnalyzer_Device/SqlOrm/src/SqlOrmManager.cpp)

`getLastCalibrationTimeByType(channel, calibrationType)` 从 `calibration_meta` 表查询：

```sql
SELECT MAX(calibrated_at) FROM calibration_meta
WHERE channel = ? AND calibration_type = ?
```

`insertCalibrationMeta` 每次校准完成时追加一条记录，查询时取对应类型的最大时间。

### 8.2 Device 端调用链

```
QML refreshCalibrationTime()
  → experiment_ctrl.getLastCalibrationTime(channel, "transmission")
    → m_dbManager->getLastCalibrationTimeByType(channel, "transmission")
  → experiment_ctrl.getLastCalibrationTime(channel, "backscatter")
    → m_dbManager->getLastCalibrationTimeByType(channel, "backscatter")
```

### 8.3 PC 端调用链

```
QML refreshCalibrationTime()
  → experiment_ctrl.getLastCalibrationTime(channel, "transmission")
    → sendRequestAndWait("get_calibration_time", {channel, calibration_type})
      ──── Control 9000 ────▶
        Device DataTransmitController: parse "get_calibration_time"
          → emit calibrationTimeRequested(channel, type, requestId)
            → ControllerManager: getLastCalibrationTime(channel, type)
              → sendCommandResult({calibrated_at: time})
      ◀──── Control 9000 ────
    → return response["calibrated_at"]
```

### 8.4 QML 属性与触发时机

**属性设计**（Device 和 PC 两端一致）：

```qml
property string lastTransCalTime: ""       // 透射光最近校准时间
property string lastBackCalTime: ""        // 背射光最近校准时间
property string currentCalibrationTime:    // 根据校准类型下拉框自动切换
    calibrationTypeCombo.currentIndex === 0 ? lastTransCalTime : lastBackCalTime
```

**触发时机**：

| 场景 | Device 端 | PC 端 |
|------|----------|-------|
| 进入页面 / 打开弹窗 | `Component.onCompleted → refreshCalibrationTime()` | `onOpened → refreshChannelOptions → refreshCalibrationTime()` |
| 切换通道下拉框 | `channelCombo.onCurrentIndexChanged → refreshCalibrationTime()` | 同上 |
| 切换校准类型下拉框 | `currentCalibrationTime` 属性绑定自动切换 | 同上 |
| 校准完成 | `onCalibrationCompleted` 信号中更新对应类型的 `lastXXXCalTime` | 同上 |

---

## 九、数据转换公式

根据项目规则文档，实验数据存储时的数据转换公式：

- **校准参考值 ref > 0**：`raw / ref * 100.0`（归一化为百分比）
- **校准参考值 ref == 0**：直接存原始值

当前实现中 `parseStoragePairs` 直接存原始值（暂不除以校准值），转换公式待后续接入。

---

## 十、关键设计决策

| 决策 | 说明 |
|------|------|
| 3 次扫描取平均 | 固定 3 轮，步长固定 20μm |
| 范围固定 0~55mm | 不允许用户调整 |
| 按索引对齐 | 3 轮扫描范围相同，同索引 = 同高度 |
| 微米整数 key | `qRound64(height * 1000)` 避免浮点精度问题 |
| 分开校准不丢失 | 先加载已有数据，新值写当前类型，另一类型保留 |
| 补齐机制 | 高度范围外的已有数据原样补回 |
| 先删后写 | clear → batchAdd，事务保护 |
| 信号推送 | Stream 9002 推送进度和完成摘要 |
| 运行时 O(1) 查询 | 等间距直接索引，无需遍历 |
| 不入库 | 原始校准数据不写入实验数据表 |
| 校准时间独立持久化 | `calibration_meta` 表按类型分别存储，`MAX(calibrated_at)` 查询 |
| 校准时间随类型切换 | QML `currentCalibrationTime` 属性绑定 `calibrationTypeCombo.currentIndex` |
| PC 端查询校准时间 | 通过 Control 9000 `get_calibration_time` 命令同步查询 Device 端数据库 |
